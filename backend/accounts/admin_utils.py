from django.contrib import admin
from django.urls import path

# Add any admin utility functions here if needed
# For now we don't need the register_view function as we're using a model-based approach